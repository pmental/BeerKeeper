# Install alongside the main app dependencies:
#   pip install -r requirements.txt -r desktop/requirements-desktop.txt
#
# pywebview's Windows backend uses the OS-provided WebView2 runtime (pinned
# by the pythonnet/clr bridge below) - no separate download needed on
# Windows 10 2004+ or Windows 11, since Microsoft ships WebView2 with the
# OS. Older Windows 10 builds may need the WebView2 Runtime installed
# separately: https://developer.microsoft.com/microsoft-edge/webview2/

pywebview==6.2.1
platformdirs==4.11.7

# Windows-only: pywebview's WebView2 backend needs pythonnet to bridge to
# it. Harmless to list unconditionally, but only actually installed on
# Windows - pip respects the environment marker.
pythonnet==3.1.0; sys_platform == "win32"

pyinstaller==6.22.2
